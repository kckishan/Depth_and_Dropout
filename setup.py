from setuptools import setup, find_packages

setup(name='Depth_Dropout',
      version='1.0',
      description='Experiments for Joint Inference for Neural Network Depth and Dropout Regularization',
      packages=find_packages()
      )